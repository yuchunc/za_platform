<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello ZaZaar!</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lato">
    <link rel="stylesheet" href="https://fonts.googleapis.com/earlyaccess/notosanstc.css"/>
  </head>

  <body data-js-view-path="<%= js_view_path(@conn, @view_template) %>" class="<%= view_name(@conn) %> <%= template_name(@view_template) %>">
    <%= render("main_nav.html", conn: @conn) %>
    <%= show_flash(@conn) %>

    <main class="main is-flex">

      <%= if false && current_resource(@conn) do %>
      <aside role="sidebar" class="main-sidebar is-flex has-background-z-dark is-collapsed">
        <%= render("sidebar.html", conn: @conn) %>
      </aside>
      <% end %>

      <div class="main-container">
        <%= render @view_module, @view_template, assigns %>
      </div>

    </main>

    <script src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
