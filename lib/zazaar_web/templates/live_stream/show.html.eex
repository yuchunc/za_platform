<div class="streaming is-flex">
  <%= render(ZaZaarWeb.LayoutView, "_stream_panel.html", conn: @conn, stream: @stream, user: @user) %>
</div>

<script charset="utf-8">

  window.streamConfig =
    {
      user_id: '<%= @user.id %>',
      stream_id: '<%= @stream.id %>',
      streamer_id: '<%= @stream.streamer_id %>',
      jwt: '<%= current_token(@conn) %>'
    }

</script>
